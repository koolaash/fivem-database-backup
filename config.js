const config = {
    webhook: "",
    time: 30, // in min

    connection: {
        host: "localhost",
        user: "root",
        password: "",
        database: "",
    },

    embed: {
        color: "GREEN"
    }
}

module.exports = config